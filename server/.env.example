# 서버 설정
API_HOST=0.0.0.0          # API 서버 호스트
API_PORT=8000            # API 서버 포트
API_DEBUG=false          # 디버그 모드 활성화 여부
ENV=dev                  # 환경 (dev, prod 등)
DEV_ENVS=dev,bx         # 개발 환경 목록 (쉼표로 구분)
CORS_ORIGINS=http://localhost:3000  # CORS 허용 출처 (쉼표로 구분)

# 파일 설정
FILE_ENCODING=utf-8      # 파일 인코딩
NOTI_TEMPLY_DIR=noti-temply  # 템플릿 디렉토리 경로